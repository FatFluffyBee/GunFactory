using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bld_Grabber : PlacedObject
{
    /*public float timer = 0.2f;
    float timerCount;

    Vector2Int grabPosition, deliverPosition;

    public override void Setup()
    {
        grabPosition = origin + PlacableObjectSO.GetDirForwardVector(dir);
        deliverPosition = origin - PlacableObjectSO.GetDirForwardVector(dir);

        Debug.Log("Setup Grabber Building");
    }

    private void Update()
    {
        timerCount += Time.deltaTime;

        if (timerCount > timer)
        {
            PlacedObject grabPlacedObject = FactoryGrid.instance.GetGridObject(grabPosition).GetPlacedObject();
            PlacedObject deliverPlacedObject = FactoryGrid.instance.GetGridObject(deliverPosition).GetPlacedObject();

            if(grabPlacedObject != null && deliverPlacedObject != null) 
            {

            }

            timerCount = 0;
        }
    }*/
}
